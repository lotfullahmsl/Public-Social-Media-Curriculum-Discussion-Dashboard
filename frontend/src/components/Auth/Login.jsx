// Login component (alternative to LoginPage)
// This component can be used if you want a separate login component
// Currently using LoginPage.jsx instead

const Login = () => {
    return <div>Login Component</div>;
};

export default Login;
